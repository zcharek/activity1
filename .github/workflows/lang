# activité 2

name: lang 
on:
  push: 
    branches:
      - "lang"

jobs: 
  french-job:
   if: vars.LANG == 'FR'
   runs-on: ubuntu-latest
   steps:
     - name: print
       run: echo "l'user ${{github.actor}} parle français." 
  
  other-job:
   if: vars.LANG != 'FR'
   runs-on: ubuntu-latest
   steps:
     - name: print
       run: echo "user ${{github.actor}} speaks english."
